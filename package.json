{
  "name": "todoist2json",
  "version": "1.0.0",
  "description": "converts todoist CSV files into JSON files",
  "main": "convert.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jan",
  "license": "ISC",
  "dependencies": {
    "@fast-csv/parse": "^5.0.0"
  }
}
